<?xml version="1.0" encoding="UTF-8"?>
<faces-config xmlns="https://jakarta.ee/xml/ns/jakartaee"
              xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
              xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                  https://jakarta.ee/xml/ns/jakartaee/web-facesconfig_4_1.xsd"
              version="4.1">

    <!--
        Application-wide resource bundle. Exposes messages.properties as
        #{msg['key.name']} in every Facelet, including <h:head> contents
        (page titles, meta tags) where a body-level <f:loadBundle> wouldn't
        yet be in scope.
    -->
    <application>
        <resource-bundle>
            <base-name>messages</base-name>
            <var>msg</var>
        </resource-bundle>
    </application>

</faces-config>
